# PIANO-TAPS
A browser-based, interactive rhythm game built using pure HTML, CSS, and JavaScript. The objective of the game is to tap on black "tiles" that move down the screen at increasing speeds. The game utilizes the browser's Document Object Model (DOM) to dynamically generate and manipulate game elements in real-time.
